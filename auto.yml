- labels: [area/hibernate]
  
  expression: |
              title_description.matches("(?is).*hibernate.*") 
              and
              !title_description.matches("(?is).*validator.*") 
  #addcomment: "/cc @maxandersen"